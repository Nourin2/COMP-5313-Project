# COMP-5313-Project
PROJECT TITLE: RACE CONDITION WITH A SHARED BUFFER

This project demonstrates race conditions in two scenarios:
(1) race condition with a shared variable (critical section problem), and
(2) race condition with a shared buffer (producer–consumer problem).

Synchronization using mutex locks and semaphores ensures that shared resources are accessed safely by concurrent threads.
